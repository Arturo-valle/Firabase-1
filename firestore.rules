rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Solo permitir acceso a usuarios autenticados.
    // Reglas más específicas se añadirán después.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}